<div layout="column" ng-cloak>


  <md-content layout-padding>
    <div>
      <form name="userForm" ng-submit="$ctrl.submit()">

        
        <div layout-gt-sm="row">
          <md-input-container class="md-block">
            <label>Name</label>
            <input ng-model="$ctrl.name">
          </md-input-container>

          <md-input-container>
            <label>Email</label>
            <input ng-model="$ctrl.email" type="email">
          </md-input-container>

          <md-input-container>
            <label>Phone Number</label>
            <input ng-model="$ctrl.phone">
          </md-input-container>
        </div>

        <md-input-container class="md-block">
          <label>Address</label>
          <input ng-model="$ctrl.address">
        </md-input-container>

        <div layout-gt-sm="row">
          <md-input-container class="md-block" flex-gt-sm>
            <label>City</label>
            <input ng-model="$ctrl.city">
          </md-input-container>

          <md-input-container class="md-block" flex-gt-sm>
            <label>State</label>
            <input ng-model="$ctrl.state">
          </md-input-container>

          <md-input-container class="md-block" flex-gt-sm>
            <label>Postal Code</label>
            <input name="postalCode" ng-model="$ctrl.postalCode" placeholder="12345"
                   required ng-pattern="/^[0-9]{5}$/">

            <div ng-messages="userForm.postalCode.$error" role="alert">
              <div ng-message="required" class="my-message">You must supply a postal code.</div>
              <div ng-message="pattern" class="my-message">That doesn't look like a valid postal
                code.
              </div>
            </div>
          </md-input-container>
        </div>


        <div layout-gt-sm="row">
          <md-input-container class="md-block">
            <label>price</label>
            <input ng-model="$ctrl.price">
          </md-input-container>

          <md-input-container>
            <label>Departing</label>
            <md-datepicker ng-model="$ctrl.submissionDate"></md-datepicker>
          </md-input-container>
        </div>


        <md-input-container class="md-block">
          <label>comments</label>
          <textarea ng-model="$ctrl.comments" md-maxlength="150" rows="5" md-select-on-focus></textarea>
        </md-input-container>

        <div>
          <md-button type="submit">Submit</md-button>
        </div>

      </form>
    </div>
  </md-content>
</div>
